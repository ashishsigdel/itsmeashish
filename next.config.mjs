/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "itsmeashish.com.np",
      "ashishsigdel.com.np",
      "www.shutterstock.com",
    ],
  },
};

export default nextConfig;
